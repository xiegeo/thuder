sudo: false
language: go

go:
  - 1.8
  - 1.11
  - tip

os:
  - linux
  - osx

env:
  - GO111MODULE=on

matrix:
  allow_failures:
    - go: tip
  fast_finish: true

script:
  - go test -v -race ./...
